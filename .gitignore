__pycache__/
*.py[cod]
.venv/
venv/
.env
.python-version
*.log
.DS_Store
# Only the small sample seed ships in the repo (deterministic, matches the README/demo).
# The full 990-row Kaggle export is not committed — add it locally per the README to run on real data.
data/seed/grocery_inventory_full.csv
